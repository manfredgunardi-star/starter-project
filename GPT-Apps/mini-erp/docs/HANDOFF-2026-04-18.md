# Handoff Mini ERP - 2026-04-18

## Status Terakhir

Mini ERP berada di `C:\Project\GPT-Apps\mini-erp`.

Fitur MVP yang sudah berjalan:

- CRUD master data inti: Pelanggan, Supplier, Produk/Jasa, Satuan, Kategori Produk, Cost Center, COA.
- Journal draft, approval, posting, posted lock, void/reversal journal.
- Kas & Bank draft, approval, posting menjadi jurnal otomatis, void/reversal.
- Buku Besar, Neraca Saldo, Laba Rugi, Neraca, drill-down ke Buku Besar.
- Closing Period untuk memblokir posting pada periode terkunci.
- Dashboard KPI dari data posted, draft approval, dan periode terkunci.
- Audit Trail viewer dengan filter query, modul, aksi, dan actor.
- Audit Trail detail drawer untuk melihat metadata, before, dan after payload.
- Approval Queue untuk draft Journal dan Kas/Bank.
- User & Role management UI untuk company membership, role, dan permission tambahan.
- Badge approval pending di TopBar dan Sidebar.
- Playwright AI ERP test agent mencakup desktop dan mobile.

Validasi terakhir:

- `npm run build` lulus.
- `npx playwright test` lulus: 52 passed.

## Perubahan Penting Terakhir

Approval:

- `src/features/approval/ApprovalQueuePage.jsx`
  - Halaman baru untuk melihat dan approve draft Journal dan Kas/Bank.
- `src/utils/approval.js`
  - Utility maker-check dan permission approval.
  - Non-owner tidak boleh approve dokumen yang dibuat sendiri kecuali punya permission `approval:self-approve`.
- `src/hooks/useApprovalPermission.js`
  - Hook UI untuk mengecek apakah dokumen bisa di-approve dan alasan blokirnya.
- `src/utils/permissions.js`
  - Role `admin` dan `accounting` mendapat `accounting:approve`.
  - `canAccess(role, permission, extraPermissions)` sekarang mendukung permission tambahan dari membership.
- `src/services/journalService.js`
  - Draft journal disimpan dengan `approvalStatus: Pending`.
  - Approval memakai `canApproveFinancialDocument`.
  - Posting tetap wajib `approvalStatus === 'Approved'`.
- `src/services/cashBankService.js`
  - Draft Kas/Bank disimpan dengan `approvalStatus: Pending`.
  - Approval memakai `canApproveFinancialDocument`.
  - Posting tetap wajib `approvalStatus === 'Approved'`.

Navigation dan route guard:

- `src/app/navigation.js`
  - Menu `Approval` ditambahkan dengan permission `accounting:approve`.
- `src/app/routes.jsx`
  - Route `/approval`.
  - `RequirePermission` sederhana untuk halaman approval.
- `src/components/layout/Sidebar.jsx`
  - Sidebar memfilter item berdasarkan permission company aktif.
- `src/components/layout/BottomNav.jsx`
  - Bottom nav juga memfilter item berdasarkan permission.

Audit Trail:

- `src/features/settings/AuditTrailPage.jsx`
  - Filter modul, aksi, actor, dan search bebas.
  - Stat card mengikuti hasil filter aktif.
- `src/services/auditService.js`
  - Mendukung localStorage audit log saat Firebase belum dikonfigurasi.
- `src/hooks/useAuditLogs.js`
  - Hook subscribe audit trail.

Testing:

- `tests/e2e/ai-erp-test-agent.spec.js`
  - Test bertambah dari 46 menjadi 52.
  - Test baru: Approval Queue, filter Audit Trail, detail Audit Trail, User & Role management, dan badge approval pending.
  - Alur posting Journal/KasBank sudah melalui approval.
- Snapshot dashboard desktop/mobile sudah diperbarui karena dashboard KPI bertambah panjang.

User & Role:

- `src/features/settings/UsersPage.jsx`
  - Halaman User & Role sekarang membaca member company dari service, bukan row hardcoded.
  - Bisa tambah/edit membership, memilih role, dan mengaktifkan permission tambahan `approval:self-approve`.
- `src/services/companyService.js`
  - Menyediakan `subscribeCompanyMembers` dan `saveCompanyMember`.
  - Mendukung Firebase path `companies/{companyId}/members` dan localStorage demo mode.
- `src/hooks/useCompanyMembers.js`
  - Hook untuk halaman User & Role.
- `src/hooks/useApprovalQueueSummary.js`
  - Menghitung jumlah approval pending dari Journal dan Kas/Bank.
- `src/components/layout/TopBar.jsx`
  - Bell menampilkan badge jumlah approval pending.
- `src/components/layout/Sidebar.jsx`
  - Menu Approval menampilkan badge jumlah approval pending.

Audit Detail:

- `src/features/settings/AuditTrailPage.jsx`
  - Row audit bisa diklik.
  - Drawer `Detail Audit` menampilkan waktu, aksi, collection, dokumen, actor, log id, metadata, before, dan after.

## Catatan Worktree

Ada banyak file modified/untracked yang merupakan bagian pekerjaan bertahap saat ini, termasuk file dari pekerjaan sebelumnya seperti Closing Period, report improvements, dashboard KPI, dan audit trail.

File untracked di luar `mini-erp`:

- `C:\Project\ERP-ACC\docs\superpowers\plans\2026-04-18-dashboard-kpis-charts.md`
- `C:\Project\ERP-ACC\erp-app\tests\dashboard.spec.js`

File deleted di luar `mini-erp` yang terlihat dari `git status`:

- `C:\Project\ERP-ACC\erp-app\test-results\.last-run.json`

File tersebut tidak terkait pekerjaan `mini-erp` dan sebaiknya diabaikan kecuali user secara eksplisit meminta menyentuh `ERP-ACC`.

## Guardrail Penting Untuk Lanjutan

- Jangan hard delete data bisnis.
- Jangan mengubah logic double-entry, mapping akun, tax, uang muka, invoice pricing, atau rekonsiliasi tanpa konfirmasi user.
- Jangan mengubah `firestore.rules`, auth config, atau Firebase config tanpa konfirmasi user.
- Jalankan minimal:
  - `npm run build`
  - `npx playwright test`
- Untuk perubahan UI yang mengubah tampilan dashboard/master-data, snapshot Playwright mungkin perlu diperbarui secara sengaja.

## Rekomendasi Lanjut Berikutnya

Sisa langkah rekomendasi immediate roadmap: 3 langkah.

1. Deactivate/reactivate member.
   - Tambahkan soft delete membership, bukan hard delete.
   - File utama: `src/features/settings/UsersPage.jsx`, `src/services/companyService.js`, `src/hooks/useCompanyMembers.js`.
   - Audit action yang disarankan: `member_deactivate`, `member_restore`.

2. Approval history per dokumen.
   - Tampilkan riwayat approval/posting/void terkait dokumen di drawer Journal dan Kas/Bank.
   - Gunakan `useAuditLogs()` atau query audit khusus per `collectionName` + `documentId`.
   - File utama: `src/features/accounting/AccountingPage.jsx`, `src/features/kas-bank/KasBankPage.jsx`.

3. Export Audit Trail.
   - Tambahkan export Excel/PDF dari hasil filter audit aktif.
   - Reuse helper export existing: `src/utils/reportExport.js`.
   - File utama: `src/features/settings/AuditTrailPage.jsx`.

## Cara Mulai Lagi

Direktori kerja:

```bash
cd C:\Project\GPT-Apps\mini-erp
```

Cek status:

```bash
git status --short
```

Validasi cepat:

```bash
npm run build
npx playwright test
```

Jalankan dev server:

```bash
npm run dev -- --host 127.0.0.1 --port 5175
```
