{
  "buildCommand": "cd ERP-ACC/erp-app && npm install && npm run build",
  "outputDirectory": "ERP-ACC/erp-app/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
